# CodeRacer - Тренажер для скоростной печати кода

CodeRacer — это онлайн-тренажер для скоростной печати программного кода. По сути, это игра, в которой пользователи соревнуются в скорости и точности набора реальных фрагментов кода на разных языках программирования.

## 🎯 Особенности

- **Игровой интерфейс**: Основной экран с фрагментом кода для набора, полем для ввода и таймером
- **Статистика в реальном времени**: Расчет и отображение скорости (WPM) и точности
- **Профиль пользователя**: Просмотр статистики и лучших результатов
- **Таблица лидеров**: Глобальный рейтинг игроков с фильтрацией по языкам программирования
- **API для фрагментов кода**: Случайные фрагменты кода из базы данных

## 🏗️ Архитектура

Проект использует классическую клиент-серверную архитектуру:

- **Бэкенд**: Node.js + Express с PostgreSQL
- **Фронтенд**: React (SPA)
- **База данных**: PostgreSQL
- **Контейнеризация**: Docker
- **CI/CD**: GitHub Actions

## 📋 Требования

- Node.js 18+
- Docker и Docker Compose
- PostgreSQL 15+ (или используйте Docker)

## 🚀 Быстрый старт

### Вариант 1: Использование Docker Compose (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd "2 task"
```

2. Запустите все сервисы:
```bash
docker-compose up -d
```

3. Приложение будет доступно по адресу:
   - Фронтенд: http://localhost
   - Бэкенд API: http://localhost:3001
   - База данных: localhost:5432

### Вариант 2: Локальная разработка

#### Настройка базы данных

1. Установите PostgreSQL и создайте базу данных:
```bash
createdb coderacer
```

2. Примените схему базы данных:
```bash
psql -d coderacer -f backend/database/schema.sql
```

#### Настройка бэкенда

1. Перейдите в директорию бэкенда:
```bash
cd backend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Запустите сервер:
```bash
npm run dev
```

Бэкенд будет доступен на http://localhost:3001

#### Настройка фронтенда

1. В новом терминале перейдите в директорию фронтенда:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` (опционально):
```bash
echo "REACT_APP_API_URL=http://localhost:3001" > .env
```

4. Запустите приложение:
```bash
npm start
```

Фронтенд будет доступен на http://localhost:3000

## 📁 Структура проекта

```
.
├── backend/                 # Бэкенд приложение
│   ├── database/           # SQL схемы
│   ├── server.js           # Основной файл сервера
│   ├── package.json        # Зависимости бэкенда
│   └── Dockerfile          # Docker образ для бэкенда
├── frontend/               # Фронтенд приложение
│   ├── public/            # Статические файлы
│   ├── src/               # Исходный код React
│   │   ├── components/    # React компоненты
│   │   ├── App.js         # Главный компонент
│   │   └── index.js       # Точка входа
│   ├── package.json       # Зависимости фронтенда
│   └── Dockerfile         # Docker образ для фронтенда
├── docker-compose.yml     # Оркестрация контейнеров
├── .github/
│   └── workflows/        # CI/CD пайплайны
└── README.md              # Документация
```

## 🔌 API Endpoints

### GET /api/snippet
Получить случайный фрагмент кода.

**Query параметры:**
- `language` (опционально): Фильтр по языку программирования

**Пример:**
```bash
curl http://localhost:3001/api/snippet?language=javascript
```

### POST /api/result
Сохранить результат гонки.

**Тело запроса:**
```json
{
  "userId": "user_123",
  "snippetId": 1,
  "wpm": 120,
  "accuracy": 95.5,
  "time": 45
}
```

### GET /api/profile/:userId
Получить профиль пользователя и статистику.

**Пример:**
```bash
curl http://localhost:3001/api/profile/user_123
```

### GET /api/leaderboard
Получить таблицу лидеров.

**Query параметры:**
- `language` (опционально): Фильтр по языку программирования
- `limit` (опционально): Количество записей (по умолчанию 10)

**Пример:**
```bash
curl http://localhost:3001/api/leaderboard?language=javascript&limit=20
```

### GET /health
Проверка здоровья сервера.

## 🐳 Docker

### Сборка образов

```bash
# Бэкенд
docker build -t coderacer-backend ./backend

# Фронтенд
docker build -t coderacer-frontend ./frontend
```

### Запуск отдельных контейнеров

```bash
# Бэкенд
docker run -p 3001:3001 --env-file backend/.env coderacer-backend

# Фронтенд
docker run -p 80:80 coderacer-frontend
```

## 🔄 CI/CD

Проект использует GitHub Actions для автоматизации:

- **Тестирование**: Автоматический запуск тестов при push и pull request
- **Сборка Docker образов**: Автоматическая сборка при успешных тестах
- **Деплой**: Готов к интеграции с облачными платформами

## 🛠️ Разработка

### Добавление новых фрагментов кода

Добавьте новые записи в таблицу `code_snippets`:

```sql
INSERT INTO code_snippets (title, language, code, difficulty)
VALUES ('Название', 'javascript', 'код здесь', 'medium');
```

### Расширение функциональности

- **Мультиплеер**: Используйте Socket.IO для режима реального времени
- **Аутентификация**: Добавьте систему регистрации и входа
- **Графики прогресса**: Интегрируйте библиотеку для визуализации (например, Chart.js)

## 📊 База данных

### Основные таблицы

- `code_snippets`: Фрагменты кода для тренировки
- `race_results`: Результаты гонок пользователей

### Индексы

Для оптимизации производительности созданы индексы на:
- `user_id` в `race_results`
- `snippet_id` в `race_results`
- `language` в `code_snippets`

## 🚧 Перспективы развития

- [ ] Таблица лидеров с фильтрацией по языкам
- [ ] Мультиплеер режим (WebSocket/Socket.IO)
- [ ] Больше контента (фреймворки, алгоритмы)
- [ ] Кастомные лобби для друзей
- [ ] Графики прогресса пользователя
- [ ] Система достижений
- [ ] Темная тема интерфейса

## 📝 Лицензия

MIT

## 👥 Авторы

Проект создан в рамках практической работы по DevOps.

