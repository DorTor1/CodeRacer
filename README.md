# CodeRacer - Тренажер для скоростной печати кода

CodeRacer — это онлайн-тренажер для скоростной печати программного кода. По сути, это игра, в которой пользователи соревнуются в скорости и точности набора реальных фрагментов кода на разных языках программирования.

## 🎯 Особенности

- **Игровой интерфейс**: Основной экран с фрагментом кода для набора, полем для ввода и таймером
- **Статистика в реальном времени**: Расчет и отображение скорости (WPM) и точности
- **Профиль пользователя**: Просмотр статистики и лучших результатов
- **Таблица лидеров**: Глобальный рейтинг игроков с фильтрацией по языкам программирования
- **API для фрагментов кода**: Случайные фрагменты кода из базы данных

## 🏗️ Архитектура

Проект использует микросервисную архитектуру с разделением ответственности:

- **Бэкенд**: Node.js + Express с PostgreSQL (основной API сервис)
- **Фронтенд**: React (SPA)
- **База данных**: PostgreSQL (общая для всех сервисов)
- **Микросервисы**: 
  - Statistics Service (статистика и аналитика)
  - Notification Service (уведомления и достижения)
- **Контейнеризация**: Docker и Docker Compose
- **CI/CD**: GitHub Actions
- **Прокси-сервер**: Nginx для маршрутизации запросов

## 🔧 Микросервисы

Проект реализует микросервисную архитектуру с двумя независимыми сервисами, которые расширяют функциональность основного бэкенда.

### Общая концепция

Микросервисы работают независимо от основного бэкенда и взаимодействуют с ним через HTTP API. Это позволяет:
- **Масштабировать** каждый сервис независимо
- **Разделить ответственность** между компонентами
- **Упростить разработку** и тестирование отдельных модулей
- **Повысить отказоустойчивость** системы

### 1. Statistics Service (Порт 3002)

**Назначение**: Сбор, хранение и анализ статистических данных о гонках пользователей.

**Функциональность**:
- Сбор общей статистики по всем гонкам (количество пользователей, средние показатели)
- Анализ статистики по конкретным языкам программирования
- Расчет персональной статистики пользователей
- Отслеживание трендов за определенный период
- Анализ результатов гонок с выдачей рекомендаций

**API Endpoints**:
- `GET /api/statistics/overview` - Общая статистика платформы
- `GET /api/statistics/language/:language` - Статистика по языку (JavaScript, Python и т.д.)
- `GET /api/statistics/user/:userId` - Статистика конкретного пользователя
- `GET /api/statistics/trends?days=7` - Тренды за последние N дней
- `POST /api/statistics/analyze` - Анализ результата гонки с рекомендациями
- `GET /health` - Проверка работоспособности сервиса

**Примеры использования**:
```bash
# Получить общую статистику
curl http://localhost:3002/api/statistics/overview

# Статистика по JavaScript
curl http://localhost:3002/api/statistics/language/javascript

# Статистика пользователя
curl http://localhost:3002/api/statistics/user/user_123

# Тренды за последние 7 дней
curl http://localhost:3002/api/statistics/trends?days=7
```

**Как работает**:
1. Основной бэкенд сохраняет результат гонки в базу данных
2. Асинхронно отправляет запрос в Statistics Service для анализа результата
3. Statistics Service анализирует данные и возвращает insights (рекомендации по улучшению)
4. Сервис использует ту же базу данных PostgreSQL для чтения статистики

### 2. Notification Service (Порт 3003)

**Назначение**: Управление уведомлениями пользователей о достижениях, рекордах и важных событиях.

**Функциональность**:
- Отправка уведомлений пользователям
- Автоматическая проверка достижений (скорость печати, точность, количество гонок)
- Проверка рекордов (новые рекорды по языкам программирования)
- Хранение истории уведомлений
- Отметка уведомлений как прочитанных

**Типы уведомлений**:
- **achievement** - Достижения (100+ WPM, 120+ WPM, идеальная точность)
- **milestone** - Вехи (10 гонок, 50 гонок и т.д.)
- **record** - Новые рекорды по языкам программирования
- **info** - Информационные уведомления

**API Endpoints**:
- `POST /api/notifications/send` - Отправить уведомление пользователю
- `GET /api/notifications/user/:userId` - Получить все уведомления пользователя
- `POST /api/notifications/check-achievements` - Проверить и выдать достижения
- `POST /api/notifications/check-records` - Проверить, является ли результат рекордом
- `PUT /api/notifications/:id/read` - Отметить уведомление как прочитанное
- `GET /health` - Проверка работоспособности сервиса

**Примеры использования**:
```bash
# Отправить уведомление
curl -X POST http://localhost:3003/api/notifications/send \
  -H "Content-Type: application/json" \
  -d '{"userId": "user_123", "type": "achievement", "message": "Отличная работа!"}'

# Получить уведомления пользователя
curl http://localhost:3003/api/notifications/user/user_123

# Проверить достижения
curl -X POST http://localhost:3003/api/notifications/check-achievements \
  -H "Content-Type: application/json" \
  -d '{"userId": "user_123", "wpm": 120, "accuracy": 95, "totalRaces": 10}'
```

**Как работает**:
1. После сохранения результата гонки основной бэкенд вызывает Notification Service
2. Сервис проверяет достижения на основе переданных параметров (WPM, точность, количество гонок)
3. Если обнаружено достижение, создается уведомление и сохраняется в памяти (в продакшене можно использовать Redis или БД)
4. Сервис также проверяет, является ли результат новым рекордом для языка программирования
5. Все уведомления доступны пользователю через API

### Интеграция микросервисов с основным бэкендом

Основной бэкенд интегрирован с микросервисами через HTTP запросы:

```javascript
// При сохранении результата гонки (POST /api/result)
// Основной бэкенд асинхронно вызывает оба микросервиса:

1. Statistics Service - анализирует результат и возвращает рекомендации
2. Notification Service - проверяет достижения и рекорды
```

**Преимущества такой архитектуры**:
- **Асинхронность**: Микросервисы вызываются асинхронно, не блокируя ответ пользователю
- **Отказоустойчивость**: Если микросервис недоступен, основное приложение продолжает работать
- **Независимость**: Каждый сервис можно обновлять и масштабировать отдельно
- **Тестируемость**: Каждый сервис можно тестировать изолированно

### Запуск микросервисов

Микросервисы автоматически запускаются при использовании Docker Compose:

```bash
docker-compose up -d
```

Все сервисы будут доступны:
- Основной бэкенд: http://localhost:3001
- Statistics Service: http://localhost:3002
- Notification Service: http://localhost:3003

Для локальной разработки можно запускать микросервисы отдельно (см. раздел "Разработка").

## 📋 Требования

- Node.js 18+
- Docker и Docker Compose
- PostgreSQL 15+ (или используйте Docker)

## 🚀 Быстрый старт

### Вариант 1: Использование Docker Compose (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd "2 task"
```

2. Запустите все сервисы:
```bash
docker-compose up -d
```

3. Приложение будет доступно по адресу:
   - Фронтенд: http://localhost
   - Бэкенд API: http://localhost:3001
   - База данных: localhost:5432

### Вариант 2: Локальная разработка

#### Настройка базы данных

1. Установите PostgreSQL и создайте базу данных:
```bash
createdb coderacer
```

2. Примените схему базы данных:
```bash
psql -d coderacer -f backend/database/schema.sql
```

#### Настройка бэкенда

1. Перейдите в директорию бэкенда:
```bash
cd backend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Запустите сервер:
```bash
npm run dev
```

Бэкенд будет доступен на http://localhost:3001

#### Настройка фронтенда

1. В новом терминале перейдите в директорию фронтенда:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` (опционально):
```bash
echo "REACT_APP_API_URL=http://localhost:3001" > .env
```

4. Запустите приложение:
```bash
npm start
```

Фронтенд будет доступен на http://localhost:3000

## 📁 Структура проекта

```
.
├── backend/                 # Бэкенд приложение (основной API)
│   ├── database/           # SQL схемы
│   ├── server.js           # Основной файл сервера
│   ├── package.json        # Зависимости бэкенда
│   └── Dockerfile          # Docker образ для бэкенда
├── frontend/               # Фронтенд приложение
│   ├── public/            # Статические файлы
│   ├── src/               # Исходный код React
│   │   ├── components/    # React компоненты
│   │   ├── App.js         # Главный компонент
│   │   └── index.js       # Точка входа
│   ├── package.json       # Зависимости фронтенда
│   └── Dockerfile         # Docker образ для фронтенда
├── microservices/         # Микросервисы
│   ├── statistics-service/ # Сервис статистики и аналитики
│   │   ├── server.js      # Основной файл сервиса
│   │   ├── package.json   # Зависимости
│   │   └── Dockerfile     # Docker образ
│   ├── notification-service/ # Сервис уведомлений
│   │   ├── server.js      # Основной файл сервиса
│   │   ├── package.json   # Зависимости
│   │   └── Dockerfile     # Docker образ
│   └── README.md          # Документация микросервисов
├── nginx/                 # Конфигурация Nginx
│   └── nginx.conf         # Настройки прокси-сервера
├── docker-compose.yml     # Оркестрация контейнеров
├── docker-stack.yml       # Docker Swarm конфигурация
├── .github/
│   └── workflows/        # CI/CD пайплайны
└── README.md              # Документация
```

## 🔌 API Endpoints

### GET /api/snippet
Получить случайный фрагмент кода.

**Query параметры:**
- `language` (опционально): Фильтр по языку программирования

**Пример:**
```bash
curl http://localhost:3001/api/snippet?language=javascript
```

### POST /api/result
Сохранить результат гонки.

**Тело запроса:**
```json
{
  "userId": "user_123",
  "snippetId": 1,
  "wpm": 120,
  "accuracy": 95.5,
  "time": 45
}
```

### GET /api/profile/:userId
Получить профиль пользователя и статистику.

**Пример:**
```bash
curl http://localhost:3001/api/profile/user_123
```

### GET /api/leaderboard
Получить таблицу лидеров.

**Query параметры:**
- `language` (опционально): Фильтр по языку программирования
- `limit` (опционально): Количество записей (по умолчанию 10)

**Пример:**
```bash
curl http://localhost:3001/api/leaderboard?language=javascript&limit=20
```

### GET /health
Проверка здоровья сервера.

## 🐳 Docker

### Сборка образов

```bash
# Бэкенд
docker build -t coderacer-backend ./backend

# Фронтенд
docker build -t coderacer-frontend ./frontend
```

### Запуск отдельных контейнеров

```bash
# Бэкенд
docker run -p 3001:3001 --env-file backend/.env coderacer-backend

# Фронтенд
docker run -p 80:80 coderacer-frontend
```

## 🔄 CI/CD

Проект использует GitHub Actions для автоматизации:

- **Тестирование**: Автоматический запуск тестов при push и pull request
- **Сборка Docker образов**: Автоматическая сборка при успешных тестах
- **Деплой**: Готов к интеграции с облачными платформами

## 🛠️ Разработка

### Добавление новых фрагментов кода

Добавьте новые записи в таблицу `code_snippets`:

```sql
INSERT INTO code_snippets (title, language, code, difficulty)
VALUES ('Название', 'javascript', 'код здесь', 'medium');
```

### Расширение функциональности

- **Мультиплеер**: Используйте Socket.IO для режима реального времени
- **Аутентификация**: Добавьте систему регистрации и входа
- **Графики прогресса**: Интегрируйте библиотеку для визуализации (например, Chart.js)

## 📊 База данных

### Основные таблицы

- `code_snippets`: Фрагменты кода для тренировки
- `race_results`: Результаты гонок пользователей

### Индексы

Для оптимизации производительности созданы индексы на:
- `user_id` в `race_results`
- `snippet_id` в `race_results`
- `language` в `code_snippets`

## 🚧 Перспективы развития

- [ ] Таблица лидеров с фильтрацией по языкам
- [ ] Мультиплеер режим (WebSocket/Socket.IO)
- [ ] Больше контента (фреймворки, алгоритмы)
- [ ] Кастомные лобби для друзей
- [ ] Графики прогресса пользователя
- [ ] Система достижений
- [ ] Темная тема интерфейса

## 📝 Лицензия

MIT

## 👥 Авторы

Проект создан в рамках практической работы по DevOps.

