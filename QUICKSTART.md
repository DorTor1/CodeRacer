# Быстрый старт CodeRacer

## 🚀 Запуск с Docker Compose (самый простой способ)

1. Убедитесь, что Docker и Docker Compose установлены:
```bash
docker --version
docker-compose --version
```

2. Запустите все сервисы одной командой:
```bash
docker-compose up -d
```

3. Подождите несколько секунд, пока все контейнеры запустятся и база данных инициализируется.

4. Откройте браузер и перейдите на:
   - **Фронтенд**: http://localhost
   - **Бэкенд API**: http://localhost:3001/health

5. Для остановки всех сервисов:
```bash
docker-compose down
```

6. Для просмотра логов:
```bash
docker-compose logs -f
```

## 📝 Проверка работы

### Проверка бэкенда:
```bash
curl http://localhost:3001/health
```

Должен вернуться JSON с `{"status":"ok"}`

### Проверка получения фрагмента кода:
```bash
curl http://localhost:3001/api/snippet
```

### Проверка базы данных:
```bash
docker exec -it coderacer-db psql -U postgres -d coderacer -c "SELECT COUNT(*) FROM code_snippets;"
```

## 🔧 Локальная разработка

Если вы хотите разрабатывать локально без Docker:

### 1. База данных
```bash
# Установите PostgreSQL и создайте базу
createdb coderacer

# Примените схему
psql -d coderacer -f backend/database/schema.sql
```

### 2. Бэкенд
```bash
cd backend
npm install
cp .env.example .env  # Отредактируйте .env при необходимости
npm run dev
```

### 3. Фронтенд
```bash
cd frontend
npm install
npm start
```

## 🐛 Решение проблем

### Порт уже занят
Если порт 80, 3001 или 5432 занят, измените порты в `docker-compose.yml`

### База данных не инициализируется
Проверьте логи:
```bash
docker-compose logs postgres
```

Удалите том и пересоздайте:
```bash
docker-compose down -v
docker-compose up -d
```

### Ошибки сборки Docker образов
Убедитесь, что все файлы на месте:
```bash
ls -la backend/
ls -la frontend/
```

## 📚 Дополнительная информация

Полная документация находится в файле [README.md](README.md)

